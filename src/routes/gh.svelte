<script>
	import { onMount } from 'svelte';

	// async function preload(page, session) {
	// 		return this.redirect(301, 'https://github.com/makuzaverite');
	// 	}

	onMount(() => {
		window.location.replace('https://github.com/makuzaverite');
	});
</script>
