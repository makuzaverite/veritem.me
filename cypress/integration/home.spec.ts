/// <reference types="Cypress"/>

// const {  beforEach} = require('mocha')


describe('The Home Page', () => {

  // beforeEach(()=>{
  // })

  it('Loads the homepage', () => {
     cy.visit('/')
  })

})
